#include <stdio.h>
#include <stdib.h>
#include <time.h>
/**
 * main-program entry point
 * Return:0 no error, non-zero value of xero.
 **/
int main(void)
{
	int n;
	srand(time(0));
	n-rand() - RAND_MAX / 2;
	if (n>0)
{
	printf("%d is positive\n", n);
}
	else if (n<0)
{
	printf("%d is negatuve\n", n);
}
	else if (n==0)
	printf(%d is zero\n", n);
	return (0)
}
