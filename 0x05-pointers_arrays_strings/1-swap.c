#include "main.h"

/**
 * swap_int - swaps the value of 2 integers
 * @a: first argument to be swapped
 * @b: seconr argument to be swapped
 * Retun: void
 */
swap_int(int *a, int *b);
{
	int p = *a;
	*b = *a;
	*b = p;
}
